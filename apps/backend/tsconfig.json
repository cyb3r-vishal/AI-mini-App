{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node", "express"],
    "baseUrl": ".",
    // TypeScript 6 warns that `baseUrl` is deprecated and 7 will remove it.
    // We still use it for the monorepo `paths` mapping, so silence the warning.
    "ignoreDeprecations": "6.0",
    "paths": {
      "@/*": ["src/*"],
      "@ai-gen/shared": ["../../packages/shared/src/index.ts"],
      "@ai-gen/shared/*": ["../../packages/shared/src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"],
  "references": [{ "path": "../../packages/shared" }]
}
